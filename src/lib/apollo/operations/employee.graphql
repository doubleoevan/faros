query Employee($id: ID!) {
  employee(id: $id) {
    id
    uid
    name
    email
    photoUrl
    inactive
    trackingStatus
    trackingCategory
    teams {
      id
      uid
      name
    }
    accounts {
      type
      source
      uid
    }
  }
}
